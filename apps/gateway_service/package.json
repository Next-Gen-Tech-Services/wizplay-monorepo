{
  "name": "wizplay-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "NODE_ENV=development ts-node-dev --respawn --transpile-only --ignore-watch node_modules --ignore-watch dist --ignore-watch build src/index.ts",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "@types/cors": "^2.8.19",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "pino": "^8.14.1",
    "pino-pretty": "^9.0.0",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  }
}
